# 変更履歴

このファイルでは現在の開発系列と、現行利用者に影響する変更を記録する。
旧形式の履歴は `documents/archive/ChangeLog.txt` と
`documents/archive/変更点.txt` に保存している。

## [Unreleased]

## [1.4.1] - 2026-08-15

### Changed

- 本体を1.4.1として公開する版番号へ更新した。
- 視聴ページ用資産の役割を判別しやすくするため、NicoCache専用ヘッダーメニューの
  JavaScriptを`05_nicocache_menu.js`へ改名した。
- 視聴ページを隠さず公式機能と同じ感覚でキャッシュを操作できるよう、固定表示していた操作群を
  公式コモンヘッダーの通知とアカウントメニューの間にあるNicoCache専用ホバーメニューへ移し、
  キャッシュ管理画面への導線も追加した。
- 利用者が導入後の操作と追加機能をすぐ確認できるよう、READMEを本体の基本操作、独立
  アップデーター、ランチャーと診断アプリ、ファーストコミット以降の機能一覧に絞って再構成した。

### Fixed

- NicoCacheメニューが公式メニューから浮いて見えないよう、開閉時のフェードを除去し、
  背景色と影を公式CommonHeaderのメニューと同じ値へ揃えた。
- 公式ヘッダーの「フォロー新着」を開いた際、Reactの再描画でNicoCacheメニューが消えたり、
  パネル内の一覧リンクをアカウント項目と誤認して移動したりしないよう、ヘッダールート直下の
  アカウント導線を優先して配置基準にした。狭幅ではポップオーバーを画面内へ収める向きに
  自動調整する。

## [1.4.0] - 2026-08-11

### Added

- 現行視聴ページへ`動画保存`、`コメント保存`、`音声のみ保存`、`キャッシュ削除`の
  操作群を追加し、完成済みキャッシュとコメントをブラウザーから直接保存できるようにした。
- 視聴ページの現行`comment.nvComment`情報から公式NVComment APIを呼び出し、
  `/cache/<動画ID>.comments.json`で動画別コメントJSONを保存するAPIを追加した。
- NicoCache_nlの管理APIと実プロキシー経路を常時監視し、3回連続のハートビート断または
  予期しないプロセス終了時に、直近約10分の監視履歴、複数回のスレッドダンプ、実行環境、
  設定、ログを自己完結HTMLへ自動保存する独立GUI診断アプリを追加した。
- 管理APIが停止した障害でも外部からスレッドダンプを取得できるよう、配布ランタイムへ
  `jcmd`を同梱し、検出直後、2秒後、5秒後の状態を比較できるようにした。
- 診断機能を実際の故障時まで検証できるよう、隔離した本体の管理API断と強制終了を発生させ、
  自動HTML生成、同梱`jcmd`による3回のダンプ、匿名化、動画情報保持、計画停止除外、
  自動再起動しないことを生成物と実プロセスで確認するE2Eを追加した。

### Changed

- 本体を1.4.0として公開する版番号へ更新した。

### Security

- 診断レポートを安全に共有しやすくするため、認証トークン、Cookie、パスワード、個人パス、
  メールアドレス、外部IP、秘密性のあるURLクエリ値を保存前に自動置換し、JKSと秘密鍵を
  収集対象外にした。動画固有の障害を特定するため、動画IDと動画タイトルは保持する。

### Fixed

- 診断アプリの状態ファイルを一時ファイルから原子的に置き換え、起動直後の読取りが
  書込み途中の空プロパティを観測してwatchdog PIDを失う競合を修正した。
- 視聴ページのReact描画がヘッダーを置き換えるとキャッシュ削除ボタンが消え、生成クラスの
  変更でも挿入できなくなる問題を修正し、SPAの動画切替と狭い画面にも追従させた。
- 旧コメントサーバー情報がなくなった現行ページでコメントXML保存が空応答になる問題を、
  現行NVCommentのJSON保存経路へ更新して修正した。
- 上流APIへJSONをPOSTするとき、UTF-8文字列の文字数をContent-Lengthへ設定して本文が
  欠落し得た問題を修正した。旧コメントXMLのダウンロードファイル名の引用符も補正した。
- 本体だけが起動して監視されない経路をなくすため、GUI、タスクトレイ、ヘッドレスCLIの
  すべてのランチャー起動で診断アプリの起動完了を確認し、画面のない環境でも隠し常駐を
  継続するようにした。
- タスクトレイの「終了」がNicoCache_nl本体まで停止していた動作を改め、本体停止、
  ランチャーのみ終了、診断アプリ終了を独立させた。起動管理画面にもランチャー終了ボタンと
  対象を明記した確認画面を追加し、別プロセスの`--headless --stop`では本体だけが停止して
  常駐ランチャーと診断アプリが残る契約を明文化した。

## [1.3.2] - 2026-08-10

### Added

- Java 17/21/25について、`actions/setup-java`の現行13配布元すべてで同一成果物を
  実行する39通りの互換性マトリクスと、配布元・VM名を表示する起動診断を追加した。
- 転送タイムアウトがJDK内部実装に依存せず、指定時間内に上流通信を中断することを
  ローカルHTTPサーバーで検証する回帰テストを追加した。

### Changed

- 本体を1.3.2として公開する版番号へ更新した。正式な外部JDK対応範囲を
  Java 17/21/25に固定し、公式ビルドと同梱ランタイムは引き続きTemurin 25とした。
- 障害調査時に現行本体のデバッグAPIから正しくスレッドダンプを
  取得できるよう、`how-to-dump-stack-trace.txt`をMarkdown形式へ変更し、
  出力内容、localhostへの直接通信、出力先、失敗時の確認方法を実装に合わせた。
- ブラウザーが`DEBUG`仮想ホストをDNSへ直接送信しないよう、新規セットアップで
  配置する`proxy.pac`にデバッグ要求をNicoCache_nlへ送る規則を追加した。

### Fixed

- 転送タイムアウトと停止処理から`sun.net.*`への反射アクセスを除去し、公開APIの
  `HttpURLConnection.disconnect()`と入力ストリームのクローズだけで中断するよう修正した。
  JDKメーカーごとの内部クラス構造差やモジュール開放指定に依存しなくなった。

### Removed

- 現行のReact検索画面とHTTPS API経路で誤った旧HTML・HTTPへの依存を
  残さないよう、`nlFilter_sys.txt`から利用元のない`SearchExtConf`、
  不要な`getthumbinfo`ヘッダ書き換え、本体の既定値と重複する動画ID設定を削除した。

## [1.3.1] - 2026-08-08

### Changed

- 本体を1.3.1、独立アップデーターを0.2.2として公開する版番号へ更新した。

### Fixed

- Windows本体MSIの既定インストール先を、意図せず追加されていた`Programs`階層から
  `%LOCALAPPDATA%\NicoCache_nl`へ修正した。誤った既定先に導入済みの場合だけ新版への
  更新時に正しい場所へ移し、利用者が選んだ任意のインストール先は保持する。
- Windows配布ランチャーの初回ウィザードでも推奨設定どおり、HTTPS MitM、ローカルCAの
  信頼登録、`proxy.pac`、ログオン時自動起動の4項目をすべて選択済みで表示するよう修正した。
- 独立アップデーターがv1.3.0の誤った既定先も探索して本体版と同梱Bouncy Castleを
  実配置から検出し、本体更新後は新しい既定先を再認識するよう修正した。
  Maven CentralではBouncy Castleの3成果物すべてに
  共通する最新版だけを選び、一部成果物の先行公開時に未公開URLへ接続して404になる
  ことを防いだ。最新版の取得失敗時も、検出済みの導入版は「不明」に戻さない。
  ビルド用依存ロックの更新処理も同じ共通版選択へ揃え、2〜4区切りの数値版を動的に
  比較して公式URLと実ファイルのSHA-256を生成するようにした。

## [1.3.0] - 2026-08-08

### Added

- 大量のフィルタリングログでプロキシ処理とGUIが詰まらないよう、NicoCacheGUIのログ配送に
  loopback限定・トークン認証付きWebSocketを追加した。ログ生成側は有界キューへ即時投入し、
  複数ログをバイナリフレームとGUI更新の両方でまとめ、遅延時は古い表示待ちログを省略して
  最新ログを優先する。

### Fixed

- 現行の共通キャッシュ表示ロジックをブラウザースクリプト試験でも読み込み、CMAF/Domandの
  品質クラスと縮小表示を実際の製品経路と同じ契約で検証できるようにした。
- Windowsのログオン時起動を平坦な配布構成どおり検証できるよう、タスクスケジューラー
  統合試験を隔離アプリケーションルートへコピーしたランチャーから実行するよう修正した。
- キャッシュ品質バッジが自分で追加したSVGとラベルを`MutationObserver`で再検出し、
  同じバッジを描画し続けてページを重くする再入ループを防いだ。同一内容のバッジ更新は
  DOM操作を省略し、同じ監視バッチに現れたリンクや関連動画も1回だけ処理する。
- `zstd`とBrotli（`br`）を本体で正式に展開し、`Accept-Encoding`の対応方式として
  `gzip`、`deflate`、`identity`とともに一元的に扱うよう修正した。将来未知の
  `Content-Encoding`を受信した場合は圧縮本文とヘッダーを壊さずそのまま転送し、
  NicoCacheGUIのログへ本体側の対応追加が必要なことを通知する。
- 現行のランキング、トップ、視聴、シリーズ、ユーザー、Nアニメなど、検索・タグ以外の
  動的動画リンクでもサムネイル情報ポップアップが表示されるようにした。SPAの遅延追加と
  `href`差し替えを追跡し、旧inline handlerとの二重発火も防ぐ。
- 現行の検索、タグ、ランキング、ユーザー投稿一覧などでキャッシュ状態を一目で
  判別できるよう、Reactで描画される動画サムネイル左下へキャッシュバッジを復活させた。
  CMAF/Domandでは`/cache/info/v2`の映像モードと音声kbpsを`1080p · 192k`のように表示し、
  狭いサムネイルでは新しい保存済み記号へ縮退する。SPAで既存リンク、遅延画像、表示幅が
  変わった場合も最新の状態へ更新する。
- 視聴ページの`fetch`監視が呼出側の`this`をネイティブAPIへ渡して再生通信を
  例外終了させないよう、元の`fetch`を`window`へ束縛してから呼び出すよう修正した。

### Changed

- `dmc`という互換フラグを現行配信方式の表示分類に使うのをやめ、旧形式とCMAF/Domandで
  リンク色を分けず、品質は新しい共通キャッシュバッジで表すよう整理した。
  `15_thumbInfoFilterCache.txt`の1か所を`true` / `false`で書き換えると、バッジの品質表示と
  記号だけの表示を切り替えられる。
- ホバーごとの外部iframe再生成とDOM再構築をなくすため、2004年版overLIBを
  NicoCache_nl専用の軽量ポップアップ実装へ置き換えた。120ms未満の通過ホバーは
  外部アクセス前に取り消し、同じ動画の通信中処理と完成DOMを再利用しつつ、
  最大12件のLRU上限でメモリ使用量を制御する。現行SPAの動画リンク処理も
  イベント委譲だけにして、初期DOM全走査とMutationObserverを廃止した。
- 既定のキャッシュ一覧スクリプトを短く保ったまま現代的なJavaScriptへ整理し、
  暗黙のグローバル変数と`document.write`に依存せず同じ一覧操作を表示するようにした。
- Windows、Linux、macOSの本体配布物を、Git追跡ファイルがclone時と同じ相対位置にある
  アプリケーションルートへ、プリコンパイル済み4 JARと専用`jre/`を重ねる構成へ統一した。
  `app/`や`lib/app/`、macOSアプリバンドルへ詰め替えず、ショートカットとログオン時
  自動起動も同梱JREから`NicoCacheLauncher.jar`を実行する。独立アップデーターも
  この平坦なZIPを検証・置換でき、ユーザーデータルートは引き続きアプリケーション
  ルートと分離する。
- 通常の開発、CI、リリース、依存更新、ネイティブパッケージのJDK/JREが混在しないよう、
  既定をEclipse Temurin 25へ統一した。Java 17/21は明示的な互換性検証だけに残し、
  配布JARのJava 11互換ターゲットは維持する。
- サムネイル情報フィルターを`09_thumbInfoFilterBase.txt`、
  `10_thumbInfoFilterLegacyLinks.txt`、`11_thumbInfoFilterVideoLinks.txt`、
  `15_thumbInfoFilterCache.txt`へ責務別に整理した。Nアニメ専用キャッシュ処理は
  全ページ共通処理へ統合し、09〜19番台のファイル名を英数字へ統一した。
- 初期設定を用途別の7ファイルへ再分類し、歴史的な番号・追加時期ではなく、
  アプリケーション、ネットワーク、動画キャッシュ、旧キャッシュ互換、書き換え、
  サムネイル、HTTPSの責務から必要な設定を探せるようにした。
- トップ、検索、タグ、ランキング、ユーザー、視聴、生放送、チャンネル、Nアニメ、
  静画の実ページで標準`local/`資産を検証し、キャッシュ済みリンクの情報取得を
  動画ごとの個別要求から最大100件の一括要求へ変更した。同一リンクの重複処理も防ぎ、
  SPAで追加されたリンクには引き続きキャッシュ品質の色を反映する。
- 視聴ページの関連動画アイコンを現行の`data-decoration-video-id`構造へ一本化し、
  URL変更を基準に`videoChanged`を通知することで、旧プレイヤーDOMに依存せず動画切替と
  キャッシュ削除対象を追跡できるようにした。

### Removed

- 現行Domand/NVComment経路を遮断する`niconicoMode`、旧検索HTML・旧サムネイル
  方式に依存する設定と製品経路、実装上効果のなかった設定、同梱本体が存在しない
  NGCommentExtension・nlMovieFetcherの初期設定を削除した。公開Extension ABIに含まれる
  旧型と保存済み旧動画キャッシュの直接配信・読取り互換は維持した。
- 現行配信方式では本体側で廃止済みの`url_injection_sys.js`、呼び出し元がなく
  Prototype.jsと旧ページDOMに依存していた`nlThumbInfoRewriter.js`、およびCSSの
  data URI化後に参照されなくなったキャッシュアイコンGIFを標準配布物から削除した。
- DMC時代の配信方式と通常/エコノミー4状態を描いた埋め込みキャッシュアイコン画像を削除し、
  CMAF/Domandの品質メタデータから組み立てるCSS/SVGバッジへ置き換えた。

## [1.2.3] - 2026-08-06

### Added

- Windows、Linux、macOSで起動管理画面をタスクトレイへ格納する`--tray`と、最小化して
  起動する`--minimized`を追加した。どちらも`--start`を併用した場合だけ本体を起動する。
- 起動方法の退行を各OSで検出できるよう、CLIの組み合わせ、WindowsタスクXML、macOS
  LaunchAgents、Linux XDG autostartと、Windows配布物の引数なし起動をCIで検証するようにした。

### Changed

- ランチャーのダブルクリックでは管理GUIだけを表示し、NicoCache_nl本体は`--start`を
  明示するか、GUIまたはタスクトレイの起動操作を行った場合だけ起動するようにした。
- ログオン時タスクは利用者の操作を妨げず本体を起動するため、全OSでランチャーへ
  `--tray --start`を渡すようにした。既存の保存形式v2は次回読込時にv3へ移行し、
  OS側の登録内容も更新する。
- 本体を1.2.3として公開する版番号へ更新した。独立アップデーターは0.2.1を維持する。

### Fixed

- ログオン時タスクのv3移行でOS側の再登録に失敗した場合、保存形式だけを先に更新して
  再試行不能にならないよう、全登録の成功後に保存形式を確定するよう修正した。
- Unixアップデーターの公開版検証がGitHub APIの制限を受けにくいよう、継続パッケージ
  検証とリリースへGitHub Actionsの認証トークンを渡すようにした。

## [1.2.2] - 2026-08-03

### Changed

- 本体を1.2.2、独立アップデーターを0.2.1として公開する版番号へ更新した。

### Fixed

- Windowsの本体ランチャーとUpdaterの説明情報を実際の役割に合わせ、ロケール依存の
  文字化けを避けるASCIIのネイティブメタデータへ修正した。Updater MSIは
  `%LOCALAPPDATA%\NicoCache_nl Updater`へユーザー単位で導入し、通常のインストールで
  不要なUAC昇格を要求しないようにした。
- UpdaterのWindowsコマンド出力を実行環境のネイティブ文字コードで読み、CLIの結果はUTF-8
  で出力するようにして、Java・Antなどの版確認とログ表示の文字化けを修正した。UTF-8や
  BOM付き出力も判定し、Ant導入後もマシンPATHを失わず、`ant.bat`を確実に再確認できるようにした。
- Bouncy Castleをjpackageの実配置先`app/lib`から検出・更新し、FFmpegはWinGetの
  `Gyan.FFmpeg`の版情報で比較するようにして、公開日を版番号として誤表示する問題を修正した。
  Bouncy Castleは標準版情報に加えてOSGiの`Export-Package`版情報も確認する。
- OS共通の依存関係境界からWindows固有処理を分離し、`WindowsDependencyManager`と
  `UnixDependencyManager`を明示的に選択する構造へ整理した。
- `how-to-dump-stack-trace.txt`を、NicoCache_nlの`DEBUG`エンドポイントから利用者データ
  フォルダーへUTF-8のスレッドダンプを取得する実際の手順へ書き直した。

## [1.2.1] - 2026-08-03

### Changed

- 初回ウィザードのHTTPS MitM、ローカルCAの信頼登録、`proxy.pac`、ログオン時自動起動を
  4つの推奨既定ON項目として分離し、HTTPS必須・通信経路の性能維持・自動起動の理由を
  画面内で説明するようにした。
- 独立アップデーターを0.2.0へ更新し、Windows、Linux、macOSのGUIとヘッドレスCLIで、
  GPAC/MP4Boxを含む依存関係を個別に更新チェック・インストールできるようにした。
  最新版の項目ではインストールを無効化し、全件操作はチェック済みで更新がある項目だけを
  対象にし、インストール後は再起動なしに状態を再検出するようにした。

### Fixed

- リリース資産をプラットフォームとパッケージ種別で確認できるよう、GitHub Releaseの
  ノートへ各資産とSHA-256の一覧表を追加し、WindowsのZIPにもチェックサムを添付するようにした。
- 継続パッケージ検証が本体の公開版番号と一致するよう、Linux/macOS向けの本体版を1.2.1へ更新した。
- Bouncy Castle依存更新で、更新対象の実差分、Gitの検証、ブランチpush、既存PRの検索・更新・
  作成結果を確認してから成功扱いにするようにし、失敗箇所をログから判断できるようにした。

## [1.2.0] - 2026-08-02

### Fixed

- 起動管理画面やタスクトレイから本体を起動したときに、本体へ誤って`--headless`を
  付けてGUIログを利用できなかったため、画面からの起動では本体GUIを表示し、
  CLIのヘッドレス動作を維持し、ログオン時自動起動も起動管理GUI経由で本体GUIを
  表示するようにした。
- クリーンチェックアウトのCIでもロック済みBouncy Castleを取得してJava 17/25ビルドへ渡すようにし、
  LinuxのDEB/RPMへCMAF/Domand変換器を保持し、macOSアプリバンドルの`Contents/app`にある
  本体JARを起動管理アプリが解決できるようにした。
- Windowsタスクスケジューラーの登録直後に行うCIのXML照会を単一XMLモードで安定させ、
  個別照会が失敗するRunnerでは空戻りと連結XMLも扱い、一意なデータルートから対象を抽出して、
  PowerShellのタスクエクスポートへもフォールバックし、任意の繰り返し要素を安全に判定するようにした。
- `/cache/info`と`/cache/info/v2`の空入力を空JSONとして扱い、`rm`、`rmtmp`、
  `echo`の引数なし・不正入力を例外ではなく400で返すようにした。`echo`のJSON
  エスケープと`getxml?type=...`の標準クエリ解析も修正し、外部APIの応答を機械的に
  扱えるようにした。
- `/cache/addlist`と`/cache/trimlist`で`list/`配下から親ディレクトリへ脱出できない
  ように検証し、プロセス外のファイルをLST APIから変更できる経路を塞いだ。
- 旧`/cache/oldrmtmp`が一時ファイル削除ではなく通常キャッシュ削除へ分岐し、
  一時ファイルだけのキャッシュで例外を起こして接続を閉じる不具合を修正した。
- 保存済みCMAFの`.cmfv`/`.cmfa`をFFmpegのHLS demuxerが拡張子不一致で拒否しないよう、
  `extension_picky`を無効化し、HLSで無効な`-safe 0`を除去した。旧FFmpegで同オプションが
  未対応の場合は互換コマンドへ一度だけ再試行するようにした。
- CI、リリース、依存関係更新が対応外のJDK 11で本体ビルドを開始して停止しないよう、
  最小サポート版のJDK 17を明示してビルドするようにした。
- Linuxの`jpackage`アプリイメージで`bin`、`lib/app`、`lib/runtime`へ分かれる配置を、
  macOSでは`Contents/Resources`を使う標準バンドル構造を、本体、初回セットアップ、
  アップデーターが正しく扱えるようにし、macOSの0.x版でも公開版番号を保持したまま
  有効な識別子と`jpackage`の制約に適合するパッケージを生成できるようにした。

- 暗号化CMAF/Domandの鍵情報を属性順に依存せず解釈し、プレイリスト内で切り替わる
  セグメントごとの鍵・IVと鍵取得の競合を正しく扱い、復号に失敗した暗号文を
  キャッシュへ保存しないようにした。
- 署名付き鍵URLの照合でNicoCache付与パラメータだけを除外し、同一パスの古い
  鍵を誤って再利用しないようにした。
- 同一CMAFセグメントの重複要求が共有する一時ファイルをセグメント単位で直列化し、
  残存した一時ファイルの切り詰めとファイルチャネルの部分書き込みにも対応した。
- 署名更新前後のCMAFプレイリストに同名セグメントがある場合も世代の異なる鍵とIVを
  混同しないよう、復号情報と待機処理を署名付きセグメントURL単位で管理するようにした。
- 同一署名付きCMAF鍵URLへの重複応答で鍵が後着順に上書きされないようにし、HTTPエラー
  応答や空の鍵本体を暗号鍵として登録しないようにした。
- 初期化や終了通知で例外が発生しても終了状態の判定と後始末を継続し、タスクトレイの
  「NicoCache_nl を終了」がタイムアウトまで待ち続ける問題を防ぐようにした。
- ユーザーデータをドキュメントへ移した構成で、未エスケープのWindowsパスを設定から
  正しく復元し、移動先の証明書不足を「起動中」と誤表示せず原因を管理画面へ示すようにした。
- CMAFキャッシュの進捗ログで、復号前・復号中の一時ファイルや完了処理後の移動元を
  保存済みサイズとして参照して表示が増減／0 byteになっていたため、保存完了セグメントと
  完成後キャッシュを使って集計するようにした。
- JDK 25の`jpackage`が自動生成するランタイムからネイティブの`java`コマンドを省略し、
  起動管理アプリが本体を子プロセスとして起動できない場合があったため、`jlink`で
  `java`／`javaw`を含む実行環境を明示生成して各OSパッケージへ渡すようにした。
- Windowsの自動起動タスクが存在しない`NicoCache_nl`サブフォルダーと壊れた`/TR`引用を
  使って登録に失敗していたため、タスクルート直下へのログオン時登録、登録直後の実在照会、
  実ユーザーIDの取得、Windowsエラーのネイティブ文字コード表示、更新・削除の回帰テストを
  追加した。間隔指定は保存・登録しない。
- 配布READMEから参照するユーザーデータ、API、更新手順、パッケージ、テスト、nlFilterの
  説明書をアプリイメージへ同じ相対配置で含め、配布後にリンク切れになる状態を修正した。

### Added

- 起動管理アプリがユーザーデータルートを起動時に読み取り専用で診断し、移行先に不足する
  フォルダー、TLSストア、MitM証明書、PACファイル、権限、旧レイアウトの混在を項目別に
  表示するようにした。ヘッドレス環境向けに`--check-data-root`と終了コードも追加した。
- 既存ユーザーが新しいユーザーデータルートへ移行する手順と、診断結果の判定基準を
  [ユーザーデータルートの診断と移行](documents/user-data-root.md)へ記録した。
- 標準nlFilterの更新前後に実行するLab検査と本体テストの手順を
  [nlFilters更新手順](nlFilters/how-to-update.md)へ記録した。
- 本体の`/cache/*`と起動管理用`/api/control/*`を引数、応答、互換エンドポイント付きで
  まとめた[本体APIリファレンス](documents/api.md)と、実ソケットで隔離検証する
  `test-api.ps1`およびCIジョブを追加した。
- ビルド、証明書生成、本体起動管理を独立したJavaアプリへ分離し、JDK 11互換の
  `NicoCacheBuild.jar`から本体・CA生成・起動管理の4成果物をマルチプラットフォームで
  生成できるようにした。起動管理はGUI、タスクトレイ、ヘッドレスCLIに対応する。
- 起動管理からログオン時に一回だけ起動するタスクを登録・編集・更新・削除できる
  Windowsタスクスケジューラー、macOS LaunchAgents、Linux XDG autostart向けの
  アダプターを追加した。
- Windows、Linux、macOSのアプリイメージ、ZIP、ネイティブパッケージへ、独立した4本の
  JavaアプリJARを同じ構成で収録し、パッケージ検証で配置と内容を確認するようにした。
- 本体へループバック限定・トークン認証の状態確認、グレイスフル停止、強制停止APIを
  追加し、証明書生成対象を`certificate-targets.txt`から読み込むようにした。
- 保存済みCMAF/Domandキャッシュの`master.m3u8`と相対セグメントをFFmpegで単一MP4へ
  変換できる独立Javaアプリを追加した。GUIのフォルダ選択・ドラッグ・アンド・ドロップ、
  出力先指定、出力先を開く操作、変換後の自動オープン、CLI／ヘッドレス実行を同じ変換
  コアで利用できるようにした。
- 本体アプリイメージ、初回セットアップ、独立アップデーターをLinuxとmacOSへ拡張し、
  LinuxではDEB/RPM、macOSではPKG/DMGとアプリイメージZIPを生成するようにした。
- Linuxの`trust`・GNOME自動プロキシー・XDG autostart、macOSの`security`・
  `networksetup`・LaunchAgentsを、初回セットアップのロールバック可能なOS連携として追加した。
- Linux/macOSのアプリイメージZIPをSHA-256検証して安全に置換する独立アップデーターと、
  対応OSのパッケージ生成・隔離起動・CLI自己診断を検証するCIを追加した。

### Changed

- ユーザーデータルート診断の全項目に用途、起動への影響、完全にするための具体的な「対応」を
  表示し、原因を調べて次の操作を判断できるようにした。LST用`list/`フォルダーについては、
  作成方法と未使用時に本体起動を妨げないことも表示する。
- 現行ニコニコ動画で必要なHTTPS MitMと`proxy.pac`を診断の対象外にせず、未設定時に
  完全ではないことと具体的な復旧方法を表示するようにした。
- リリースワークフローが、対象タグに対応する`CHANGELOG.md`のセクションをリリースノートへ引用するようにした。
- 置き換えが完了した`RunNicoCache.ps1`、`NicoCache_nl.sh`、`genCerts.bat`、
  `genCerts.sh`、空の旧ランチャーバッチを削除し、配布パッケージと開発手順の入口を
  独立Javaアプリへ統一した。クリーン環境からビルドするための`build-javac.ps1`/
  `build-javac.sh`だけはブートストラップとして残した。
- 保存済みCMAF/Domand変換器を本体のWindows/Linux/macOSパッケージとアプリイメージZIPへ
  同梱し、各パッケージ作成時にJava 11互換のJARを再生成して、実行時ルートの
  `tools/cmaf-to-mp4/`へ配置するようにした。FFmpeg本体は引き続き同梱しない。
- リリースワークフローへLinux/macOS本体・独立アップデーターの各プラットフォーム／
  アーキテクチャ別パッケージとSHA-256を追加した。Solarisは対応対象に含めない。
- Windows／Linux／macOS配布ワークフローの変更検知対象を、独立アプリのソース、ビルド入口、
  配布README、データ資材、ユーザーデータ文書まで広げ、入力変更時にパッケージが取りこぼされない
  ようにした。

### Removed

- GUIログの用途が重複していた`Debug`タブを削除し、デバッグログも`Main`タブへ表示するようにした。
  旧`Debug`タブの検索履歴は起動時に整理する。

## [1.1.0] - 2026-07-30

### Changed

- Windows向け配布物へ移行済みであることを明確にし、GitHub Releaseから本体JARと
  そのSHA-256ファイルを個別アセットとして公開しないようにした。
- GUI設定へ検索内容を混在させないよう、ログ検索履歴を利用者データフォルダーの
  専用`NicoCacheGUI.search-history.properties`へ移し、従来の
  `NicoCacheGUI.property`内の履歴を初回起動時に自動移行するようにした。

### Added

- システム資材と利用者データを迷わず配置できるよう、初回ウィザードへ
  ユーザーデータ保存先の選択画面を追加し、アプリ側 `config.properties` の
  `userDataRoot` へ絶対パスを保存するようにした。
- 配置分離の退行をCIで検出できるよう、システム・利用者nlFilterの実行順、
  `local` の上書きとフォールバック、ディレクトリリンク配信を実JARで検証し、
  既存のCMAF受信・キャッシュ作成・オフライン再生試験と一緒に実行するようにした。

- デバッグ中に必要なログだけを追えるよう、main、debug、Extensionの各ログタブへ
  行単位の即時検索を追加した。通常文字列と正規表現、大文字小文字区別、件数と
  正規表現エラー表示、解除操作、タブ別の日時付き検索履歴に対応した。
- 問題報告用ログをGUIから安全に採取できるよう、ログ画面へデバッグ記録の
  チェックボックスを追加した。記録中はアプリケーションJARと同じフォルダーの
  `debug.log`へ出力し、設定を`NicoCacheGUI.property`へ保存する。

### Fixed

- `local/features` など正当に配置したシンボリックリンクが実体の配置先を理由に
  拒否されないよう、URLの各パス要素でトラバーサルを拒否した上でリンク先を
  配信できるようにした。
- クリーンcheckoutでもE2EとWindows配布物がTLSクライアント用トラストストア、
  CORS設定例を確実に利用できるよう、Git除外規則と必須配布検査を修正した。
  JDK 25の厳格警告対応後も初回ウィザードを検査できるよう、
  ネスト後の実クラス名へ追従した。
- JDK 25で検出されるコンストラクタ初期化順、GUI状態の直列化対象、FLVの
  2 GiB超ファイル位置、LRU最小容量への縮小処理の不整合を解消した。
- `/local/` 配信でパーセントエンコードした区切り文字、`.`、`..`を含む
  URLパスを拒否し、シンボリックリンクの利用を妨げずにパストラバーサルを
  防ぐようにした。
- 巨大・不正・曖昧なHTTPヘッダーによるメモリ消費とrequest smugglingを防ぐため、
  ヘッダー全体と1行の上限、フィールド名、重複`Content-Length`、
  `Transfer-Encoding`を厳密に検証するようにした。
- 上流プロキシーへ接続できないときに無応答で切断しないよう、原因を示す本文を
  伴う502応答を利用者へ返すようにした。
- `/local/` のHEAD応答が誤って405または本文長0にならないよう、GETと同じ
  `Content-Length`を本文なしで返し、範囲外Rangeには416を返すようにした。
- 壊れたGUI位置・サイズ設定でログ画面が1×1や画面外にならないよう、最小サイズと
  仮想画面内への補正、正数設定のフォールバックを追加した。
- GUI操作をEDTから呼び出してもデッドロックや例外にならないよう、Extensionタブ
  追加とGUI解放のスレッド切替を修正した。
- 上流の `Cache-Control`、`Age`、`Expires` を無視してメモリキャッシュを再利用
  しないよう、キャッシュ可能期間の計算と期限切れ走査の並行処理を修正した。
- `Cache-Control`がない正常な応答をメモリキャッシュへ保存するときの
  `NullPointerException`を防ぎ、外部サムネイルを正常に再利用できるようにした。
- 長時間のデバッグでもディスクを圧迫しないよう、GUIのチェックボックスと
  `DebugMode=true`の両方で`debug.log`の古い履歴を切り詰め、ファイルを
  1 MiB以内に保つようにした。
- Windows InstallerワークフローがMSIビルド後の代入文を引数として解釈しないよう、
  `AppVersion`行末の不要なPowerShell行継続を削除した。
- MSI更新ではアプリ側設定を保持しつつ、通常アンインストール後にMSI管理外の
  `config.properties`が製品フォルダーへ残らないようにした。
- HLSキャッシュの品質指定URLが常に最適品質へ置き換わらないよう、映像モードと
  音声ビットレートが一致する完成済みキャッシュを選択するようにした。
- CMAFチャンクの破損や競合した一時ファイルを保存しないよう、暗号文と復号後の
  データ長、16進数の暗号情報、音声抽出コマンドの終了状態を検証するようにした。
- テンプレート末尾の `$` で例外が発生しないよう、キーワード境界判定を修正した。
- Java 17、21、25のいずれで独立アップデーターをビルドしても不要な警告が出ないよう、
  永続化しないGUIレンダラーの直列化警告を局所的に抑制した。
- JDK 25がMSIロケール名の英字大小を正規化しても日本語ローカライズを正しく検証
  できるよう、独立アップデーターのパッケージテストを修正した。

### Changed

- 標準 `local`、標準nlFilter、Extensionサンプルをアプリケーション側に保持し、
  システム側を先、利用者側を後に読み込む二層構成へ変更した。書き込みは
  `userDataRoot` 側だけに行い、配布するシステム資材を
  `packaging/system-files.txt` で一元管理する。
- 未リリースの利用者データ分離方式を整理し、`NICOCACHE_DATA_ROOT` 環境変数と
  `nicocache.dataRoot` Javaシステムプロパティ、システム資材の利用者領域への
  初回コピーを廃止した。
- 標準フィルターとnlFilter Labを本体の版・配布物と一体で更新できるよう、
  `roflsunriz/nlFilters`の全履歴を`nlFilters/`へ統合し、リリースとWindows配布CIが
  同じコミット内のフィルターを使用するようにした。外部管理の100番台リンクは
  引き続きGit管理外とする。
- 本体インストーラーと独立アップデーターを一覧上で判別できるよう、共用していた
  `niconico-0.ico` から、導入と循環更新をそれぞれ表す用途別アイコンへ分離した。
- Javaコンパイル警告を見逃さないよう、機能、初回セットアップ、E2E、GUIの
  全テストを`-Xlint:all -Werror`で固定し、WindowsのJDK 25でも実JARとGUIの
  E2Eを継続実行するようにした。
- ローカルキャッシュ管理ページのテンプレートを更新時刻・サイズ・文字コードが
  変わるまで再利用し、ファイル更新時には自動で読み直すようにした。
- 配信方式の判定を排他的な `classic`、`dmc`、`domand` 状態で管理し、同時に
  成立し得る2つの真偽値へ依存しないようにした。
- ソース内のTODO等を監査し、現行挙動に必要な項目を実装するとともに、Extension
  ABI、Processor登録順、既存フィルター構文の互換性から維持する判断をコメントへ
  明記した。
- MSIの同梱JavaランタイムをJDK 25へ統一し、リリース、WindowsインストーラーCI、
  依存更新後のパッケージ検証で同じJDK 25の実行環境を生成するようにした。
- 独立アップデーターのTemurin選択でJava 25を推奨・既定にし、GUI表示とCLIの依存更新対象を一致させた。
- 独立アップデーターのビルド用JDKも25へ統一し、CIとパッケージ生成スクリプトでJDK 25以外を拒否するようにした。
- ZIPとMSIが同じWindowsアプリイメージを配布するよう、共通ペイロード生成へ統一した。
  開発時に必要なソース、テスト、ビルド・検証スクリプト、資料は `development/` に
  同梱し、ZIP展開物と共通アプリイメージの内容一致テストを追加した。
- ビルド経路を `build-javac.ps1` に統一し、旧Ant用ビルドスクリプトとバッチを廃止した。
- GUI起動経路をJDK選択とJAR検証を備えた `RunNicoCache.ps1` に統一し、旧Starter
  バッチと旧CUIバッチを廃止した。現行ツリーに存在しないオーバーレイ用ビルド
  スクリプトも削除した。
- `build-javac.ps1` で検出できた JDK 17、21、25 の `javac` を選択して本体を
  ビルドできるようにし、CIなどの無指定実行では検出された最新版を自動選択する。
- `RunNicoCache.ps1` でも検出できた JDK 17、21、25 の `javaw` を選択して
  起動できるようにした。
- Java 25 LTSを利用者が選べるよう、Windows、Linux、macOSでビルド、基本動作、
  TLS、Extension ABI、初回セットアップを継続検証し、アップデーターの対応
  Temurin一覧へJava 25を追加した。推奨LTSもJava 25とする。
- 現行の利用手順と変更履歴を見つけやすくするため、`README.md` と
  `CHANGELOG.md` を正規の入口に統一し、旧版の README、変更履歴、開発メモを
  `documents/archive/` へ整理した。HTTPS MitM の手順は `documents/tls.md` に集約した。
- リリース ZIP に旧版資料を混入させないよう、`documents/archive/` 以下を
  アーカイブ収集対象から除外した。

### Added

- 実際の製品JARを隔離領域から別プロセス起動し、正常系・異常HTTP・並行利用・
  上流停止・復旧を実ソケットで検証するE2Eと、実Swing GUIの全動的要素、
  レスポンシブ描画、状態遷移、永続化、リソース解放を検証するGUI E2EをCIへ
  追加した。
- 表示言語に依存せずGUI操作を検証し支援技術から識別できるよう、ログ画面、
  タブ、テキスト領域、スクロール領域、全右クリック項目へ安定した
  コンポーネント名とアクセシブル名を追加した。
- リポジトリ管理の依存関係を安全に保守できるよう、GitHub Actionsを完全な
  コミットSHAへ固定してDependabotで週次確認し、Bouncy Castleの安定版、
  公式配布元、ライセンス、実ファイルのSHA-256を検証した更新PRを自動作成する
  処理と、ロック・更新レポートの回帰テストを追加した。
- Windowsパッケージの更新・修復・再インストールから利用者の設定やローカル資産を
  独立させるため、既定の「ドキュメント」配下に `NicoCache_nl` 利用者データ
  フォルダーを設け、旧配置の設定、`local/`、`nlFilters/`、Extensionを
  上書きせず移行する共通パス解決と移行処理を追加した。明示データルートと
  `portable.flag` による従来配置も維持する。
- 独立アップデーターを本体と同じGitHub Releaseから安全に導入できるよう、
  `updater/VERSION` の版番号で自己完結MSIを生成し、SHA-256とともに既存の
  ZIP、JAR、本体MSI資産へ追加するリリース処理と資産集合の契約テストを追加した。
- リリースタグのソースと版番号から検証済みMSIを配布できるよう、
  `v<major>.<minor>.<build>` タグでWindowsインストーラーとSHA-256を生成し、
  ZIPおよびJARと同じGitHub Releaseへ自動添付する処理を追加した。
- インストール後すぐ起動できる導線として、MSIによるデスクトップ
  ショートカットの作成、修復、更新、アンインストール時の削除処理を追加した。
- Windowsインストール後の操作を4画面にまとめるため、HTTPS証明書、Windows自動
  プロキシー、ログオン時自動起動を明示選択でき、適用後に3項目それぞれの
  成功・失敗・未選択を確認できる初回起動ウィザードと、適用失敗時に設定
  ファイルとOS変更を復元して結果画面から再試行できる処理を追加した。
- 現行環境を汚染せず初回セットアップを検証できるよう、全画面操作と
  ファイルロールバックをフェイク環境で確認するテスト、および一時CIでCA生成、
  証明書ストアの不変性、プロキシーと自動起動の実適用・完全復元を確認する
  試験を追加した。
- JavaやPowerShell 7を利用者が事前導入せずに済むよう、専用Javaランタイム、
  単一の製品ランチャーを含むWindowsアプリイメージとMSIの生成を追加した。
- 現行環境を汚染せずパッケージを検証できるよう、TLS MitMとOS設定変更を無効化
  した隔離アプリイメージ試験と、一時GitHub Actionsランナー上でのMSI無人
  インストール・起動・アンインストール試験を追加した。
- 証明書生成用依存関係の取り違えを防ぐため、Bouncy Castle 1.85の取得元と
  SHA-256を固定し、パッケージ生成時に検証する依存ロックを追加した。
- 継続的インテグレーションで JDK 11 のビルド、機能テスト、Extension ABI 互換
  テストを自動実行し、`v*` タグからシンボリックリンクを除く配布ファイル一式の
  ZIP、JAR、SHA-256 チェックサムを公開する GitHub Actions を追加した。
- 不要コード整理による退行を検出するため、実プロキシー、条件付き取得、上流
  障害、本文書換え、コメント、サムネイル、DOMAND/CMAF の完成・オフライン
  再生、`hlsext` の暗号化セグメント復号、HTTPS TLSループバック、
  `/cache/*`、MP4・FLV・SWF・旧HLSキャッシュ、Extensionライフサイクルを
  隔離環境で確認する機能テストを追加した。
- 未知の外部 Extension のバイナリ互換性を維持するため、全 `dareka.*` の
  public/protected ABI と同梱 Extension のコンパイルを検証するテストを追加した。

### Removed

- Java 11 以降では必ずスキップされていた Java 8 以前向け PATCH 許可リスト
  書換え処理と、効果のない `useWorkaroundForAllowedMethods` 設定を削除した。
- 保守時に実装と誤認しないよう、置換済みのコメントアウトコードを削除した。
- 現行ニコニコ動画で使用されないSmile、DMC単一ファイル、DMC-HLSの新規取得
  Processor登録と取得実装、getflvレスポンス書換えを削除した。配布済み
  Extension用のpublic/protected ABIは互換shimで維持する。
- 旧取得経路だけに作用していた `disableStreamingWarning`、
  `useSmileCacheInsteadOfDmcEconomy`、`useSmileCacheInsteadOfDmc`、
  `ignoreEmbeddedPlayer`、`noLiveCache`、`deletedMoviePlayMode`、
  `mapFileUpdatingInterval` と、上流SWF変換用の `swfConvert*` 既定設定を削除した。

### Fixed

- Java 25の `jpackage` が作る最小ランタイムでも日本語設定を読み込めるよう、
  Windowsパッケージへ `jdk.charsets` を明示的に同梱し、`EUC-JP` が見つからず
  起動できない問題を修正した。またJDK 25で変更されたWiXヘルパー定義へ対応する
  テンプレートをJDK 17用と分離し、どちらのJDKでもMSIを生成できるようにした。
- Windowsアプリイメージへネストした資料を配置できるよう、配布ファイルの移動前に
  移動先の親ディレクトリを作成し、インストーラーCIが途中終了する問題を修正した。
- 独立アップデーターMSIの公開後も本体更新で正しい配布物を取得できるよう、
  GitHub Release上の任意のMSIではなく、リリースタグと一致する本体MSIと
  対応SHA-256だけを選択するよう修正した。
- Windows更新後にユーザーPATHからWindowsAppsが欠落した環境でもWinGetを
  利用できるよう、Javaから通常ファイルとしては追跡できないApp Execution Aliasの
  reparse pointをリンク自体の存在で検出し、絶対パスから起動するよう修正した。
- WinGetに存在するTemurinを公式配布APIへ誤ってフォールバックさせないよう、
  全パッケージへのユーザースコープ強制をやめ、パッケージが対応するスコープを
  WinGetに選択させるよう修正した。あわせてWinGet未提供のApache Antを
  「WinGet不成立」と表示せず、公式配布API対象として明示するよう修正した。
- MSI更新時に `jpackage` の導入先再帰削除がキャッシュや利用者設定まで消さない
  よう、生成されたWiX定義から再帰削除だけを除外して製品ファイル単位で更新する
  よう修正した。また既定外の導入先でもアンインストール時のWindows設定復元を
  実行し、製品ファイルを正しい場所から削除できるよう、実際の導入先を
  インストーラー管理情報へ保存してMSI標準の導入先へ復元する。
- MSIの修復が実際に配布ファイルを復元することを検証できるよう、修復試験で
  元MSIと `REINSTALL=ALL` を指定して配布ソースから全ファイルを再配置し、
  既定外の導入先も明示するよう修正した。アンインストールにはMSIの
  プロパティーテーブルから取得した製品コードを使い、失敗時に原因を追跡
  できるよう最初の例外と後始末例外を保持する操作別の詳細ログも追加した。
- 信頼済みルートCAを利用者の承認なしに登録しないよう、初回セットアップから
  Windows標準の証明書登録を開始し、OSの許可画面で承認された場合だけ登録する
  よう修正した。画面のない自動試験では登録を無効化し、CA生成と証明書ストアが
  変わらないことを検証する。
- 英語環境のリリースCIでも日本語ファイル名を含むMSIを生成できるよう、
  MSI生成ロケールとデータベースを日本語コードページ932へ固定し、製品説明を
  ASCIIへ分離した。あわせてアンインストール復元コマンドをMSIの文字数上限内へ
  短縮した。
- MSIのアンインストールで初回セットアップが登録したCA証明書と変更した
  Windowsプロキシー設定が残らないよう、信頼済みルートCAを非対話対応の
  `X509Store` APIで削除してから保存済みの変更前状態へ戻し、自動起動も同時に
  復元する処理を追加した。
- ログオン時の自動起動で作業ディレクトリが製品ルート以外になっても設定や
  既定ファイルを見失って終了しないよう、単一の製品ランチャーを製品ルートから
  一度だけ自己再起動する処理を追加した。
- MSIからスタートメニューや直クリックで初回ウィザードを起動した場合でも、
  設定、証明書、既定ファイルなどの実行時データをEXEと同じ製品ルートへ配置し、
  証明書生成先もそこへ固定することで、`ca.cer` が見つからずセットアップが
  ロールバックされ、その後の本体起動も設定を読めない問題を修正した。
- checkout を行わない release の publish job でも GitHub CLI が対象リポジトリを
  解決できるよう、release 作成時にリポジトリ名を明示するよう修正した。
- Windows で直前の配信が保持するファイルハンドルが解放される前にキャッシュ削除
  API が呼ばれても、一時的な削除失敗で `NG` にならないよう短時間の削除再試行を
  追加した。
- Extension の定期イベント通知スレッドが JVM の終了を妨げないよう、サーバー
  停止時に対象スレッドを割り込ませて終了するよう修正した。

### Changed

- Windowsパッケージでは設定、GUI状態、証明書、キャッシュ、状態ファイルを
  利用者データフォルダーへ保存し、更新対象の既定設定、セットアップスクリプト、
  ネイティブライブラリはアプリ本体から参照するよう変更した。アンインストールは
  Windows設定だけを復元し、利用者データを保持する。
- 初回起動ウィザードを製品GUIなしでも利用できるよう、GUIと同じ適用・
  ロールバック処理を使う `NicoCache_nl.exe --setup --headless` を追加し、
  証明書生成専用EXEを廃止してアプリランチャーを完全に1本化した。
- 作業開始時の共通指針見落としを防ぐため、調査やコマンド実行より前に `COMMON-AGENTS.md` を先頭から末尾まで読み、EOFを確認する必須ゲートを追加した。

- Windowsインストーラーで旧版を同じ製品として安全に更新できるよう、
  Upgrade UUIDを固定し、CIのMSI試験を無人導入だけでなく修復、ユーザー状態を
  保った更新、起動抑止、ショートカット削除、アンインストール後のOS状態復元まで
  拡張した。
- リリース ZIP に同一リポジトリで管理する `nlFilters` の `01`〜`20` 番台 `.txt` を
  含め、配布時に標準フィルタを同梱できるようにした。
- 旧取得経路の実装を削減しながら既存Extensionをロードできるよう、公開ABIを
  変更せず、旧ProcessorとSWF上流変換クラスを非登録の互換shimへ変更した。
- 既存のFLV・SWF・MP4・旧HLS・CMAFキャッシュを引き続き利用できるよう、
  キャッシュ走査、`/cache/*`、HLS変換、現行CMAF内の旧HLS応答処理を維持した。
