# nlフィルタ定義(文字コード判定用なのでこの行は削除しないこと)
# 2026-08-10 現在、必要なシステム置換はないため、ルールを意図的に定義していない。
#
# - 動画ID種別は本体の既定値とキャッシュ索引で解決する。
# - 検索ページはReactで描画され、旧SearchExtensionのHTML挿入位置は存在しない。
# - getthumbinfoは本体がhttps://ext.nicovideo.jp/api/getthumbinfo/を直接使用する。
#
# 本ファイルはEasyRewriterがシステムフィルタとして読み込むため、
# ファイル自体と先頭の文字コード判定行は削除しないこと。
